/* Declaration only: preserves the primitive for backend packing diagnostics.
 * No behavioral substitute or physical timing model is provided here. */
(* blackbox *) module ODDRXE(input SCLK, RST, D0, D1, output Q);
 parameter GSR="ENABLED";
endmodule
